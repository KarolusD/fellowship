---
name: fellowship:using-worktrees
description: Loads the using-worktrees skill — startup checks, builder commit contract, and the orchestrator's eager-merge protocol for isolated agent dispatches.
---

# Using Worktrees

Loads the `using-worktrees` skill for in-session work. See [`skills/using-worktrees/SKILL.md`](../skills/using-worktrees/SKILL.md) for the full mechanics — when Gandalf dispatches into `.claude/worktrees/agent-<hash>`, what every agent runs at startup, the builder's commit contract, and the orchestrator's eager merge after DONE.
