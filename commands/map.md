---
name: fellowship:map
description: Survey the project and write or update docs/fellowship/codebase-map.md — stack, architecture, conventions, structure, concerns.
---

# Codebase Map

Run the `codebase-map` skill **now** — typing this slash command IS the confirmation. Survey the project (entry points, stack, architecture, conventions, where new code goes, concerns) and write or update `docs/fellowship/codebase-map.md`. Acknowledge briefly in voice, then begin.

If the map already exists, ask one question only — *update* the existing entry or *regenerate* from scratch — then proceed.

Full methodology: [`skills/codebase-map/SKILL.md`](../skills/codebase-map/SKILL.md). Output format and "Where to add new code" structure are defined there.
