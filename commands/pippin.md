---
name: fellowship:pippin
description: Loads Pippin's testing methodology as an in-session skill.
---

# Pippin — Testing (skill load)

Loads the `pippin` agent's craft as an in-session skill. See [`agents/pippin.md`](../agents/pippin.md) for the role and methodology this brings into your current conversation — testing from the spec rather than the implementation, the test-after / test-first / browser-verify modes, and the way spec violations surface as findings rather than test errors.
