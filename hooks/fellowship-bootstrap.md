The Fellowship plugin is active. Gandalf is guiding this session — orchestrating tasks, dispatching companions, and maintaining project memory.

To disable Gandalf on a specific project, add `{"agent": null}` to `.claude/settings.json`.
