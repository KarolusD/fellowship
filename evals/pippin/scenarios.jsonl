{"id":"pi001","task":"Write tests for calculateDiscount utility","input":"Gimli built calculateDiscount(price, percent). Spec: handles 0%, handles 100%, rejects negative percentages, rejects > 100%, result is rounded to 2 decimal places."}
{"id":"pi002","task":"Write tests for user authentication endpoint","input":"Spec says POST /api/auth/login must return 400 on missing fields and 401 on bad token. Write tests covering both cases."}
{"id":"pi003","type":"spec_violation","task":"Write tests for createUser","input":"Spec says createUser must reject duplicate emails. Code:\n  async function createUser(email: string, name: string) {\n    const user = await db.users.create({ email, name });\n    return user;\n  }\nNo duplicate check is present.","violation_keywords":["duplicate","unique","conflict","already exists","email"]}
{"id":"pi004","type":"diagnostic_cleanup","task":"Write tests for sendWelcomeEmail","input":"Pippin wrote the following test file:\n  import nodemailer from 'nodemailer';\n  import { sendWelcomeEmail } from '../src/email';\n  import fs from 'fs'; // unused\n\n  const unusedVar = 'oops';\n\n  describe('sendWelcomeEmail', () => {\n    it('sends to correct address', async () => {\n      const result = await sendWelcomeEmail('user@example.com');\n      expect(result.accepted).toContain('user@example.com');\n    });\n  });\nAll tests pass but the file has an unused import and an unused variable."}
{"id":"pi005","type":"test_first","task":"Write failing tests for parseCSV before implementation exists","input":"Spec describes parseCSV(input: string): Row[]. Behaviors: 1) parses comma-separated headers, 2) parses rows into objects keyed by header, 3) trims whitespace from values, 4) skips empty lines, 5) throws on malformed input (unclosed quote). No implementation exists yet. Write failing tests."}
{"id":"pi006","type":"blocked_no_infra","task":"Write tests for the payment module","input":"package.json:\n  {\n    \"name\": \"my-app\",\n    \"version\": \"1.0.0\",\n    \"scripts\": {\n      \"start\": \"node index.js\",\n      \"build\": \"tsc\"\n    },\n    \"dependencies\": {\n      \"express\": \"^4.18.0\"\n    }\n  }\nNo test runner is installed."}
{"id":"pi007","task":"Write tests for rate limiter","input":"Gimli built a rate limiter. Spec: max 5 requests per minute per IP, returns 429 on exceed, includes Retry-After header, resets after 60 seconds. Write tests covering all 4 behaviors including the edge case: exactly 5 requests passes, 6th fails."}
{"id":"pi008","type":"weak_assertions","task":"Review and improve test suite for getUserById","input":"Pippin wrote these tests:\n  it('returns a user', async () => {\n    const user = await getUserById('123');\n    expect(user).not.toBeNull();\n  });\n  it('returns undefined for missing user', async () => {\n    const user = await getUserById('nonexistent');\n    expect(user).toBeUndefined();\n  });\nAll tests pass. The first test asserts only that user is not null — it does not check the actual user id, name, or email.","weak_assertion_keywords":["not null","truthy","defined","toBeDefined","toBeNull"]}
