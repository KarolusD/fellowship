{"id":"pi009","task":"Write tests for sendNotification","input":"Spec says sendNotification must handle: email delivery failure (retry once), missing recipient (return error), rate limit exceeded (queue for later). Write tests covering all three paths."}
{"id":"pi010","type":"spec_violation","task":"Write tests for processPayment","input":"Spec says errors must be logged and re-thrown. Code:\n  async function processPayment(orderId: string, amount: number) {\n    try {\n      const result = await stripe.charges.create({ amount });\n      return result;\n    } catch (e) {\n      // swallowed\n    }\n  }\nErrors are silently swallowed — neither logged nor re-thrown.","violation_keywords":["catch","silent","ignored","rethrow","re-throw","log"]}
